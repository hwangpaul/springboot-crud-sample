<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sample.crud.dao.UserDAO">

    <resultMap id="UserResultMap" type="com.sample.crud.vo.UserVO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="address" column="address"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT id, name, email, phone, address, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        SELECT id, name, email, phone, address, created_at, updated_at
        FROM users
        ORDER BY created_at DESC
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT id, name, email, phone, address, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM users
        WHERE email = #{email}
    </select>

    <insert id="insert" parameterType="com.sample.crud.vo.UserVO" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        <!-- Use sequence for Oracle -->
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            SELECT user_seq.NEXTVAL FROM dual
        </selectKey>
        INSERT INTO users (
            id, name, email, phone, address, created_at, updated_at
        ) VALUES (
            #{id}, #{name}, #{email}, #{phone}, #{address}, SYSDATE, SYSDATE
        )
    </insert>

    <update id="update" parameterType="com.sample.crud.vo.UserVO">
        UPDATE users
        SET name = #{name},
            email = #{email},
            phone = #{phone},
            address = #{address},
            updated_at = SYSDATE
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
